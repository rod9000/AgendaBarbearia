services:
  api:
    image: atendai/evolution-api:v1.8.7
    container_name: evolution_api
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      API_KEY: minha_chave_super_secreta_123
      API_PORT: 8080
      LOG_LEVEL: error
      TYPE: wwebjs
    volumes:
      - evolution_instances:/evolution/instances

volumes:
  evolution_instances:
